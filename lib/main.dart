import 'package:flutter/material.dart';

void main() {
  runApp(const ByteZeroTestApp());
}

class ByteZeroTestApp extends StatelessWidget {
  const ByteZeroTestApp({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: Scaffold(
        body: Center(
            child: Text("To be implemented")
        ),
      ),
    );
  }
}
